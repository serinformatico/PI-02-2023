/*
    EJERCICIO 3: Convertir a arrow con 2 o mas parámetros
    Ahora nuestro trabajo será refactorizar la función saludar() que recibe un parámetro,
    es decir, escribirla con la sintaxis necesaria para convertirla en una arrow function.
*/

const saludar = (nombre, apellido) => 'Hola, ' + nombre + ' ' +  apellido + '!';

console.log(saludar("Juan", "Pérez"));